# Version Management

## Versioning Strategy

- **Semantic Versioning**: `vX.Y.Z`
- **Git Tags**: Used for releases.

## Release Process

1. Update version in `wails.json`.
2. Update version in `package.json`.
3. Create git tag.
4. CI/CD pipeline (GitHub Actions) builds artifacts.
